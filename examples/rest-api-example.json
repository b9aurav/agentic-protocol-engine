{
  "name": "REST API Load Test Example",
  "description": "Production-ready configuration for testing a REST API application",
  "applicationType": "rest-api",
  "config": {
    "projectName": "ecommerce-api-test",
    "targetUrl": "https://api.ecommerce.example.com",
    "targetPort": 443,

    "agentCount": 50,
    "testDuration": 15,
    "testGoal": "Simulate realistic e-commerce user journeys including browsing products, adding to cart, and completing purchases",
    "endpoints": [
      "/api/v1/health",

      "/api/v1/products",
      "/api/v1/products/{id}",
      "/api/v1/cart",
      "/api/v1/cart/items",
      "/api/v1/orders",
      "/api/v1/orders/{id}",
      "/api/v1/users/profile",
      "/api/v1/users/addresses"
    ],

  },
  "expectedBehavior": {
    "authentication": "None (public API)",
    "sessionManagement": "Stateless public API",
    "typicalUserFlow": [
      "Browse product catalog",
      "Search for specific products",
      "View product details",
      "Check product availability",
      "Browse categories",
      "View user profiles (public data)"
    ],
    "performanceTargets": {
      "averageResponseTime": "< 200ms",
      "p95ResponseTime": "< 500ms",
      "errorRate": "< 1%",
      "throughput": "1000+ requests/minute"
    }
  },
  "recommendations": {
    "agentScaling": "Start with 10-20 agents, scale up to 100+ for stress testing",
    "testDuration": "Run for 10-30 minutes for meaningful results",
    "monitoring": "Focus on API response times, error rates, and database performance",
    "endpoints": "Include both read-heavy (product browsing) and write-heavy (order creation) operations"
  }
}