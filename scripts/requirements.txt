# Performance Validation Dependencies for APE
# Required for task 10.2: Validate performance targets and KPIs

# Core dependencies
docker>=6.0.0
httpx>=0.24.0
psutil>=5.9.0
structlog>=22.0.0

# Prometheus metrics parsing
prometheus-client>=0.16.0

# Data processing and analysis
pyyaml>=6.0
asyncio-mqtt>=0.11.0

# Optional: Enhanced logging and monitoring
rich>=13.0.0
click>=8.0.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0